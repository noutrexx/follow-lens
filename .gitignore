# ── Secrets & personal data — NEVER commit ──
# config.json holds your sessionid (auth token) and tracked accounts
config.json
# scraped follower/following snapshots (personal)
data/
# generated dashboards embed real usernames
viewer.html
frontend/viewer.html
# cached login sessions
sessions/
*.log
.last_scan

# ── Python ──
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/

# ── OS / editor ──
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
